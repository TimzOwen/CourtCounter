<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.basketball">

    <application
        android:allowBackup="true"
        android:icon="@drawable/house1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OurSchoolActivity"></activity>
        <activity android:name=".GamesScoresActivity" />
        <activity android:name=".OurSuccessActivity" />
        <activity android:name=".SportsDirectorsActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".KabuSportsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>